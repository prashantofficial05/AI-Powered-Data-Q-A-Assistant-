# AI Data Q&A Assistant - Python dependencies
# Install with:  pip install -r requirements.txt
#
# FIX: removed strict == version pins that caused pip to fail when
# newer SDK versions (groq 1.x, openai 2.x, anthropic 0.10x) are
# already installed. Using >= allows pip to satisfy deps correctly.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
sqlalchemy>=2.0.30
pymysql>=1.1.1
python-dotenv>=1.0.1
pandas>=2.2.2
tabulate>=0.9.0
httpx>=0.27.0
openai>=1.30.1
groq>=0.8.0
anthropic>=0.28.0
aiofiles>=23.2.1
pydantic>=2.7.1
cryptography>=42.0.7
